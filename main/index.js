import React from 'react';
import {style} from './index.scss';

const Main = ({}) => {

    return (
        <div className={style}>
            This is a test
        </div>
    );
};

Main.propTypes = {

};

export default Main;
